<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Dashboard - Cabo Fit Pass</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; color: #333; }
        .container { max-width: 1200px; margin: 0 auto; padding: 20px; }
        .header { background: white; border-radius: 20px; padding: 30px; margin-bottom: 30px; box-shadow: 0 15px 35px rgba(0,0,0,0.1); display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; }
        .nav-link { padding: 10px 20px; background: linear-gradient(135deg, #667eea, #764ba2); color: white; text-decoration: none; border-radius: 25px; margin: 5px; }
        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-bottom: 30px; }
        .stat-card { background: white; padding: 25px; border-radius: 15px; box-shadow: 0 10px 25px rgba(0,0,0,0.1); text-align: center; }
        .stat-icon { font-size: 2.5rem; color: #667eea; margin-bottom: 15px; }
        .stat-number { font-size: 2rem; font-weight: bold; color: #333; }
        .stat-label { color: #666; font-size: 0.9rem; text-transform: uppercase; }
        .section { background: white; border-radius: 20px; padding: 30px; margin-bottom: 30px; box-shadow: 0 15px 35px rgba(0,0,0,0.1); }
        .section-title { font-size: 1.5rem; color: #333; margin-bottom: 20px; }
        .booking-card { border: 2px solid #f0f0f0; border-radius: 15px; padding: 20px; margin: 15px 0; }
        .booking-title { font-size: 1.2rem; font-weight: bold; margin-bottom: 10px; }
        .btn { padding: 8px 16px; border: none; border-radius: 8px; font-weight: 500; cursor: pointer; text-decoration: none; margin: 5px; }
        .btn-primary { background: linear-gradient(135deg, #667eea, #764ba2); color: white; }
        @media (max-width: 768px) { .header { flex-direction: column; text-align: center; } .stats-grid { grid-template-columns: repeat(2, 1fr); } }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div>
                <h1>Welcome, <%= profile.full_name || 'Fitness Enthusiast' %>!</h1>
                <p><%= profile.email || 'demo@cabofit.com' %></p>
            </div>
            <div>
                <a href="/" class="nav-link"><i class="fas fa-home"></i> Home</a>
                <a href="/classes" class="nav-link"><i class="fas fa-search"></i> Classes</a>
                <a href="/dashboard" class="nav-link"><i class="fas fa-user"></i> Dashboard</a>
            </div>
        </div>

        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon"><i class="fas fa-calendar-check"></i></div>
                <div class="stat-number"><%= stats.totalBookings %></div>
                <div class="stat-label">Total Bookings</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon"><i class="fas fa-clock"></i></div>
                <div class="stat-number"><%= stats.upcomingBookings %></div>
                <div class="stat-label">Upcoming</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon"><i class="fas fa-coins"></i></div>
                <div class="stat-number"><%= stats.monthlyCredits - stats.creditsUsed %>/<%= stats.monthlyCredits %></div>
                <div class="stat-label">Credits Left</div>
            </div>
        </div>

        <div class="section">
            <h2 class="section-title"><i class="fas fa-history"></i> My Bookings</h2>
            <% if (bookings && bookings.length > 0) { %>
                <% bookings.forEach((booking) => { %>
                    <div class="booking-card">
                        <div class="booking-title"><%= booking.classes.title %></div>
                        <p><i class="fas fa-map-marker-alt"></i> <%= booking.classes.gyms ? booking.classes.gyms.name : 'Cabo Fitness' %></p>
                        <p><i class="fas fa-calendar"></i> <%= new Date(booking.classes.schedule).toLocaleString() %></p>
                        <p><i class="fas fa-credit-card"></i> <%= booking.type %> - <%= booking.payment_status %></p>
                        <% if (booking.notes) { %><p><i class="fas fa-comment"></i> "<%= booking.notes %>"</p><% } %>
                    </div>
                <% }); %>
            <% } else { %>
                <div style="text-align: center; padding: 40px;">
                    <p>No bookings yet. <a href="/classes" class="btn btn-primary">Browse Classes</a></p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>